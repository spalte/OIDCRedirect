{
  "name": "oidcredirect",
  "version": "1.0.0",
  "description": "OIDC Redirect is a small Node server that allows [OHIF](https://ohif.org), or other OIDC enabled apps, running on a host that has already been authenticated by some other means (ie. local login, VM displayed only to an authenticated user, etc.) to immediately connect to a Google Cloud Healthcare PACS.",
  "repository": {
    "type": "git",
    "url": "git://github.com/spalte/OIDCRedirect.git"
  },  
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "lint": "eslint . --fix"
  },
  "author": {
    "name" : "Joël Spaltenstein",
    "email" : "spalte@naturalimage.ch"
  },
  "license": "MIT",
  "dependencies": {
    "axios": "^0.21.1",
    "jsonwebtoken": "^8.5.1",
    "node-rsa": "^1.1.1",
    "pem-jwk": "^2.0.0"
  },
  "devDependencies": {
    "eslint": "^7.18.0",
    "eslint-config-airbnb-base": "^14.2.1",
    "eslint-plugin-import": "^2.22.1"
  }
}
